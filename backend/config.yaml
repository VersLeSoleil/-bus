# config.yaml
database_connection:
  host: "127.0.0.1"
  port: 3306
  user: "root"
  password: "Iamahandsomeboy2002"
  name: "schoolBus"

server:
  port: ":8888"


database_names:
  admin_db: "schoolbus"
  passenger_db: "??????" # *******请填你们的db名
  driver_db: "driver_db" # 同上


jwt:
  expiration_hours_passenger: 1
  expiration_hours_admin: 9999
  expiration_hours_driver: 24


